# Orion MCP v3 — instalar no venv:  pip install -r requirements.txt
#
# Inclui dependências do pacote (pyproject.toml) + ferramentas para aplicar
# migrações PostgreSQL sem cliente psql instalado no sistema.

# --- Bancos de dados / cache ---
asyncpg>=0.30.0
asyncmy>=0.2.9
pymysql>=1.1.2
sqlalchemy==2.0.47
redis>=5.2.0

# --- API / servidor ---
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
httpx>=0.28.0
pyyaml>=6.0.2

# --- MCP / IA ---
mcp>=1.2.0
fastmcp>=2.2.0
openai>=1.59.0

# --- Filas / workers ---
celery[redis]>=5.4.0

# --- gRPC ---
protobuf>=6.31.1,<7.0.0
grpcio>=1.62.0
grpcio-tools>=1.62.0

# --- Dados / notebooks ---
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.7.2
jupyter>=1.0.0

# --- Relatório semanal de OS (agente_analise_os) ---
reportlab>=4.0.0
Pillow>=10.0.0
schedule>=1.2.0

# --- Config / testes ---
python-dotenv>=1.0.0
pytest-asyncio>=1.3.0
